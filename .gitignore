node_modules/
dist/
.env
.env.local

# Uploaded patient documents
uploads/

# Build artifacts and caches
.turbo/
coverage/
playwright-report/
test-results/
apps/api/public/
*.tsbuildinfo

# Cliente de Prisma generado — se regenera con `prisma generate` (y en el build
# del Dockerfile). Versionarlo ensucia el diff en cada generate y metía binarios
# nativos de Windows (.dll.node) inútiles en el contenedor Linux.
apps/api/prisma/generated/

# Dump DDL de Supabase: artefacto derivado, se desincroniza de schema.prisma
modelo.sql

# Índice local de la herramienta Atlas
.atlas/

# Impeccable: estado efímero de live mode y ficheros locales con secretos.
# DESIGN.md y .impeccable/design.json SÍ se versionan: son documentación.
**/.impeccable/live/sessions/
**/.impeccable/live/roots.json
**/.impeccable/live/inject-journal.json
**/*.local.json
**/*.local.md

# Logs de ejecuciones sueltas
apps/api/out.txt
apps/api/push_log.txt
apps/api/seed_log.txt

# Packs de skills de agentes instalados localmente (no son parte del producto)
.claude/skills/
.agents/skills/
